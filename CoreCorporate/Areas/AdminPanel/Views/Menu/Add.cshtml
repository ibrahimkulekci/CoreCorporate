@model EntityLayer.Concrete.Menu
@{
    ViewData["Title"] = "Add";
    Layout = "~/Areas/AdminPanel/Views/Shared/_AdminLayout.cshtml";
}
@section styles{
    <link href="~/admintheme/css/plugins/iCheck/custom.css" rel="stylesheet">
}
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Yeni Menü Ekle</h2>
    </div>
    <div class="col-lg-2">

    </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox ">
                <div class="ibox-content">
                    <div class="row">
                        <div class="col-sm-12">
                            @using (Html.BeginForm("Add", "Menu", FormMethod.Post))
                            {
                                <div class="form-group">
                                    @Html.Label("Menü Adı")
                                    @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.Name, null, new { @class = "text-danger" })
                                </div>
                                <div class="form-group">
                                    @Html.Label("Menü Adresi")
                                    @Html.TextBoxFor(x => x.MenuUrl, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.MenuUrl, null, new { @class = "text-danger" })
                                </div>
                                <div class="form-group">
                                    @Html.Label("Menü Üst ID")
                                    @Html.TextBoxFor(x => x.MenuParentID, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.MenuParentID, null, new { @class = "text-danger" })
                                </div>
                                <div class="form-group">
                                    @Html.Label("Menü Sıralaması")
                                    @Html.TextBoxFor(x => x.MenuDisplayOrder, new { @class = "form-control" })
                                    @Html.ValidationMessageFor(x => x.MenuDisplayOrder, null, new { @class = "text-danger" })
                                </div>
                                <div class="form-group">
                                    @Html.Label("Yayınlansın mı?")
                                    <div class="checkbox i-checks">
                                        @Html.CheckBoxFor(m => m.MenuStatus)
                                    </div>
                                    @Html.ValidationMessageFor(m => m.MenuStatus, null, new { @class = "help-block" })
                                </div>
                                <div>
                                    <button class="btn btn-primary float-right" type="submit"><strong>Yayınla</strong></button>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section scripts{
    <!-- iCheck -->
    <script src="~/admintheme/js/plugins/iCheck/icheck.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.i-checks').iCheck({
                checkboxClass: 'icheckbox_square-green',
                radioClass: 'iradio_square-green',
            });
        });
    </script>
}