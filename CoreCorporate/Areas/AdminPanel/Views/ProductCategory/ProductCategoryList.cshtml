@using EntityLayer.Concrete
@model List<ProductCategory>
@{
    ViewData["Title"] = "ProductCategoryList";
    Layout = "~/Areas/AdminPanel/Views/Shared/_AdminLayout.cshtml";
}
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Tüm Ürün Kategorileri</h2>
    </div>
    <div class="col-lg-2">

    </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox ">
                <div class="ibox-title">
                    <a href="~/AdminPanel/ProductCategory/ProductCategoryAdd" class="btn btn-primary btn-sm">Yeni Ürün Kategorisi Ekle</a>
                </div>
                <div class="ibox-content">
                    @if (Model.Count <= 0)
                    {
                        <h2 class="text-center">Henüz ürün kategorileri eklenmemiştir!</h2>
                    }
                    else
                    {
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Ürün Kategori Adı</th>
                                    <th>Ürün Kategori Url</th>
                                    <th>Ürün Kategori Durumu</th>
                                    <th>Ürün Kategori Son Düzenleme</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.ProductCategoryID</td>
                                        <td>@item.ProductCategoryTitle</td>
                                        <td>@item.ProductCategoryUrl</td>
                                        <td>@Html.Raw(item.ProductCategoryStatus == true ? "<span class='badge badge-success'>Yayında</span>" : "<span class='badge badge-danger'>Yayında Değil</span>")</td>
                                        <td>@(((DateTime)item.ProductCategoryUpdatedDate).ToString("dd.MM.yyyy"))</td>
                                        <td> <a href="~/AdminPanel/ProductCategory/ProductCategoryUpdate/@item.ProductCategoryID" class="btn btn-w-m btn-primary">Düzenle</a> </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</div>